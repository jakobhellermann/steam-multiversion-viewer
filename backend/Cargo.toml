[package]
name = "steam-multiversion-viewer"
version = "0.1.0"
edition = "2024"


[features]
default = ["unity"]
# Optional unity-asset support: serializedfile/bundle parsing via rabex,
# the `levelN`/`*.assets`/`globalgamemanagers` transformer dispatch, and
# anything else in `crate::unity`. Disable to build a viewer with zero
# unity-specific code.
unity = ["dep:rabex-env", "dep:rabex-env-steam-depot-vfs"]

[dependencies]
anyhow = "1.0"
arc-swap = "1.9"
axum = { version = "0.8", default-features = false, features = ["tracing", "http1", "json", "macros", "matched-path", "tokio", "query"] }
camino = { version = "1.2.2", features = ["serde1"] }
directories = "5"
futures-util = "0.3"
mime_guess = "2.0.5"
parking_lot = "0.12"
serde = { version = "1.0", features = ["derive"] }
similar = "2.6"
thiserror = "2.0"
serde_json = "1"
serde-value = "0.7"
steam-depot-vfs = { git = "https://github.com/jakobhellermann/steam-depot-vfs" }
steam-vent = "0.5"
steam-vent-depot = { git = "https://github.com/jakobhellermann/steam-vent-depot" }
steam-vent-proto = "0.5"
tokio = { version = "1.52", features = ["rt-multi-thread", "macros", "net", "sync", "signal", "time", "process"] }
tokio-stream = { version = "0.1", features = ["sync"] }
tower = { version = "0.5", features = ["timeout"] }
tower-http = { version = "0.6", features = ["trace"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
utoipa = { version = "5", features = ["axum_extras"] }
utoipa-axum = "0.2"
zstd = "0.13.3"
time = { version = "0.3.47", features = ["formatting", "macros"] }
rabex-env = { version = "0.1.0", git = "https://github.com/jakobhellermann/rabex-env", optional = true, features = ["tracing-instrument"] }
rabex-env-steam-depot-vfs = { version = "0.1.0", git = "https://github.com/jakobhellermann/rabex-env", optional = true, features = ["tracing-instrument"] }

# FUSE mount is linux-only.
[target.'cfg(target_os = "linux")'.dependencies]
steam-depot-mount = { git = "https://github.com/jakobhellermann/steam-depot-vfs" }
