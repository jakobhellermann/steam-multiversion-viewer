STEAM_USERNAME=
STEAM_PASSWORD=

# Point the native window at the vite dev server (pnpm dev, port 6555)
# instead of the embedded frontend — hot reload, while vite keeps
# proxying /api to this backend on 6556.
# SMV_FRONTEND_URL=http://127.0.0.1:6555
